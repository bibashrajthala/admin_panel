export const selectOrders = (state) => state.order?.orders;

export const selectOrder = (state) => state.order?.order;

export const selectUpdateStatus = (state) => state.order?.updateStatus;

export const selectInvoices = (state) => state.order?.invoices;

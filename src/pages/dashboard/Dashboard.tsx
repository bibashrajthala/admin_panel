import React from "react";
import LayoutApp from "../../components/layoutApp/LayoutApp";

type Props = {};

const Dashboard = (props: Props) => {
  return <LayoutApp page="dashboard" />;
};

export default Dashboard;
